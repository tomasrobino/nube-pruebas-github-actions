name: Git action 1

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run tests
      run: |
        echo "Running tests"
        java --version
        ls
        javac src/Hola.java
        java src/Hola.java